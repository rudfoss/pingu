{
  "name": "@radtools/web-api",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "build:tsoa": "ts-node scripts/buildTSOA.ts",
    "build:swaggerui": "ts-node scripts/buildSwaggerUI.ts",
    "build:code": "ts-node scripts/build.ts",
    "build": "yarn build:tsoa && yarn build:swaggerui && yarn build:code",
    "serve": "node dist/index.js"
  },
  "dependencies": {
    "express": "^4.17.1",
    "swagger-ui-express": "^4.1.6"
  },
  "devDependencies": {
    "@types/swagger-ui-express": "^4.1.2",
    "tsoa": "^3.8.0"
  }
}
